import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amirrezamdi23.planner',
  appName: 'دفترچه‌ی روزانه',
  webDir: 'dist'
};

export default config;
